import { access, readFile } from 'node:fs/promises';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..');const required=['assets/growth.css','assets/visual-network.svg','assets/visual-workshop.svg','insights/index.html','insights/eu-ai-act-readiness.html','insights/why-ai-pilots-fail.html','insights/ai-assistant-security-baseline.html'];for(const f of required)await access(path.join(root,f));const js=await readFile(path.join(root,'assets/site.js'),'utf8');for(const token of ['AI Risk & Readiness Snapshot','Useful thinking for responsible AI decisions','visual-network.svg','insights-grid'])if(!js.includes(token))throw new Error(`Missing growth token: ${token}`);console.log('Growth, insights and visual assets validated.');